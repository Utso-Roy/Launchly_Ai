import React from 'react';
import { AuthContext } from '../../AuthProvider/AuthProvider';

const Home = () => {

    return (
        <div>
            This is home route 
        </div>
    );
};

export default Home;